# Hermes adapter

Hermes supports MCP servers over stdio, which fits GeoPin well.

1. Install GeoPin in a virtual environment.
2. Add the example server config from `config.example.yaml` to your Hermes config.
3. Start Hermes and ask which MCP-backed tools are available.
