# OpenClaw adapter

Copy this folder into your OpenClaw skills directory, for example:

```bash
mkdir -p ~/.openclaw/workspace/skills/geopin
cp SKILL.md ~/.openclaw/workspace/skills/geopin/SKILL.md
```

Then start a new session or restart the gateway so OpenClaw picks up the skill.
